{
  "Logging": {
    "LogLevel": {
      // we commented this out because we control this using program argument
      //"Default": "Information",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  }
}
